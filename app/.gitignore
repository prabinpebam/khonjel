# Build output
dist
dist-ssr

# Storybook static build (design-system inventory)
storybook-static

# Electron packaging output
release

# Electron main/preload build output (generated from electron/main/*.ts by build-electron)
electron/main.cjs
electron/preload.cjs

# Dependencies
node_modules

# Downloaded native runtimes + model weights (fetched by scripts/fetch-llama.mjs etc.)
# Anchored to the app root so they do NOT match source dirs like electron/main/models/.
/vendor
/models

# Test / eval artifacts
coverage
eval/screens
eval-results
playwright-report
test-results
.playwright

# Local
*.local
*.log
.DS_Store
.tmp
